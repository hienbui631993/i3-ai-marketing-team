# i3 AI Marketing Team — environment variables (copy to .env and fill in)
# .env is gitignored; never commit real keys.

# Google AI (Gemini) API key for the nano-banana image-generation MCP
# (used by the Social Creative Designer skill). Get one from Google AI Studio.
# NOTE: the @ycse/nanobanana-mcp package reads GOOGLE_AI_API_KEY (not GEMINI_API_KEY).
GOOGLE_AI_API_KEY=your-google-ai-api-key-here
